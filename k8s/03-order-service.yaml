apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: tubes-devops
spec:
  replicas: 1
  selector:
    matchLabels: { app: order-service }
  template:
    metadata:
      labels: { app: order-service }
    spec:
      containers:
      - name: order-service
        image: daffaaliefta/order-service:v1.0.0
        ports: [{ containerPort: 3003 }]
        resources:
          requests: { cpu: "50m", memory: "64Mi" }
          limits:   { cpu: "100m", memory: "128Mi" }
        livenessProbe:
          httpGet: { path: /health, port: 3003 }
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet: { path: /health, port: 3003 }
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: tubes-devops
spec:
  selector: { app: order-service }
  ports: [{ port: 80, targetPort: 3003 }]
  type: ClusterIP
