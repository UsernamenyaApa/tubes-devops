apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: tubes-devops
spec:
  replicas: 1
  selector:
    matchLabels: { app: user-service }
  template:
    metadata:
      labels: { app: user-service }
    spec:
      containers:
      - name: user-service
        image: daffaaliefta/user-service:v1.0.0
        ports: [{ containerPort: 3001 }]
        resources:
          requests: { cpu: "50m", memory: "64Mi" }
          limits:   { cpu: "100m", memory: "128Mi" }
        livenessProbe:
          httpGet: { path: /health, port: 3001 }
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet: { path: /health, port: 3001 }
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: tubes-devops
spec:
  selector: { app: user-service }
  ports: [{ port: 80, targetPort: 3001 }]
  type: ClusterIP
